# Copy to .env and adjust as needed.

# Storage
DATA_DIR=data
DATABASE_PATH=data/jobs.db
OUTPUT_DIR=data/outputs
WEIGHTS_DIR=data/weights

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DATABASE=0

# Worker / Real-ESRGAN
DEFAULT_MODEL=realesrgan-x4plus
USE_GPU=true
TILE_SIZE=0
DOWNLOAD_TIMEOUT_SECONDS=60
MAX_IMAGE_BYTES=52428800

# Cleanup
# Delete the downloaded input image once a job finishes.
DELETE_INPUT_AFTER=true
# Remove finished jobs (DB rows + result files) older than N hours. 0 disables.
RESULT_TTL_HOURS=24

# Webhooks (per-request callback_url in POST /api/upscale)
# Shared secret used to sign deliveries. Convention: prefix with "whsec_".
# Leave empty to send unsigned webhooks.
WEBHOOK_SECRET=
WEBHOOK_TIMEOUT_SECONDS=10
WEBHOOK_MAX_RETRIES=3
# Public base URL so webhook payloads carry an absolute result link.
# e.g. https://upscale.example.com  (empty => relative /api/jobs/.../result)
PUBLIC_BASE_URL=

# Cloudflare Tunnel (named tunnel, profile "tunnel" only).
# Token from the Zero Trust dashboard; leave empty if you only use tunnel-quick.
CLOUDFLARE_TUNNEL_TOKEN=
